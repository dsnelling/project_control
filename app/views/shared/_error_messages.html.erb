<% if target.errors.any? %>
  <!-- to show the validation errors in forms -->
  <div id="errorExplanation">
    <h2> <%= pluralize(target.errors.count, "error") %> prohibited this record
      from being saved/updated</h2>
    <ul>
      <% target.errors.full_messages.each do |msg| %>
        <li><%= msg %>
	  <% end %>
	</ul>
  </div>
<% end %>

